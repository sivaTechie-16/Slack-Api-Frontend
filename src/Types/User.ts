export interface Users {
    id: number;
    userName: string;
    response: string;
    amount: number;
    timestamp: string;
  }

  export interface Report {
    lunchCount_userName:string;
    totalAmount:number;
    yesCount:string
  }